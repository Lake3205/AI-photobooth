stages:
  - build
  - deploy

build_dev:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev"'
      when: always
    - when: never
  script:
    - docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}"
    - docker build -t ${DOCKER_USERNAME}/dark-tech-frontend-dev:dev ./frontend
    - docker build -t ${DOCKER_USERNAME}/dark-tech-backend-dev:dev ./backend
    - docker push ${DOCKER_USERNAME}/dark-tech-frontend-dev:dev
    - docker push ${DOCKER_USERNAME}/dark-tech-backend-dev:dev


deploy_dev:
  stage: deploy
  needs:
    - job: build_dev
  tags:
    - hva
  image: ubuntu:latest
  before_script:
    - apt-get update && apt-get install -y openssh-client
  script:
    - echo "$VPS_SSH_KEY" | tr -d '\r' > id_rsa
    - chmod 400 id_rsa
    - ssh -o StrictHostKeyChecking=no -i id_rsa root@185.228.82.235 "
        docker pull ${DOCKER_USERNAME}/dark-tech-frontend-dev:dev &&
        docker pull ${DOCKER_USERNAME}/dark-tech-backend-dev:dev &&
        cd /home/dark-tech &&
        docker-compose -f docker-compose.dev.yml up -d --remove-orphans
      "
    - rm id_rsa
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev"'
      when: always
    - when: never

build_main:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: always
    - when: never
  script:
    - docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}"
    - docker build -t ${DOCKER_USERNAME}/dark-tech-frontend:latest ./frontend
    - docker build -t ${DOCKER_USERNAME}/dark-tech-backend:latest ./backend
    - docker push ${DOCKER_USERNAME}/dark-tech-frontend:latest
    - docker push ${DOCKER_USERNAME}/dark-tech-backend:latest

deploy_main:
  stage: deploy
  needs:
    - job: build_main
  image: ubuntu:latest
  tags:
    - hva
  before_script:
    - apt-get update && apt-get install -y openssh-client
  script:
    - echo "$VPS_SSH_KEY" | tr -d '\r' > id_rsa
    - chmod 400 id_rsa
    - ssh -o StrictHostKeyChecking=no -i id_rsa root@185.228.82.235 "
        docker pull ${DOCKER_USERNAME}/dark-tech-frontend:latest &&
        docker pull ${DOCKER_USERNAME}/dark-tech-backend:latest &&
        cd /home/dark-tech &&
        docker-compose -f docker-compose.prod.yml up -d --remove-orphans
      "
    - rm id_rsa
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      when: always
    - when: never

.test_connection_template: &test_connection
  stage: deploy
  tags:
    - hva
  image: ubuntu:latest

  before_script:
    - apt-get update && apt-get install -y openssh-client

  script:
    - echo "$VPS_SSH_KEY" > id_rsa
    - chmod 400 id_rsa
    - ssh -v -o StrictHostKeyChecking=no -i id_rsa root@185.228.82.235 "echo '✅ SSH connection successful!'"
    - rm id_rsa

test_ssh:
  <<: *test_connection
  rules:
    - if: '$CI_COMMIT_BRANCH == "dev"'
      when: manual
    - when: never